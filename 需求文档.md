# 一日三餐饮食热量统计 App 需求文档

## 1. 项目概述

### 1.1 项目名称
**Calorie Tracker** — 一日三餐饮食热量统计

### 1.2 项目目标
开发一个简易的全栈 Web 应用，帮助用户记录每日三餐所吃食物的热量，统计每日摄入总量，并给出饮食建议。

### 1.3 技术栈

| 层级 | 技术 | 版本 | 备注 |
|------|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 | Java 8 兼容，原计划 3.x 因 JDK 版本降级 |
| ORM | MyBatis-Plus | 3.5.5 | boot-starter（非 spring-boot3-starter） |
| 数据库 | MySQL | 8.0 | 驱动 mysql-connector-j 8.0.33 |
| 前端框架 | Vue 3 + Vite | 5.x | Composition API + TypeScript |
| UI 组件库 | Element Plus | 2.5.x | |
| HTTP 客户端 | Axios | 1.6.x | Vite proxy 转发 /api 到后端 8080 |
| JDK | Java 8 | 1.8.0_381 | Oracle Corporation |
| 构建工具 | Maven | 3.9.8 | 阿里云镜像加速 |

---

## 2. 功能需求

### 2.1 食物库管理
- 查看所有食物列表，支持按**名称关键词搜索**和**分类筛选**
- 添加新食物（名称、分类、单位热量、单位、默认份量、蛋白质、脂肪、碳水）
- 编辑已有食物信息
- 删除食物

### 2.2 餐食记录
- 选择日期（默认当天）
- 选择餐别：**早餐（BREAKFAST）、午餐（LUNCH）、晚餐（DINNER）**
- 搜索食物并添加到当前餐别，填写食用份量
- 自动计算该食物小计热量 = 食物单位热量 × 份量
- 查看当天已记录的三餐汇总
- 删除某条餐食记录

### 2.3 每日热量概览（Dashboard）
- 日期选择器，默认当天
- 三个卡片展示早/午/晚三餐各自的热量汇总
- 当日总热量显示
- 三大营养素（蛋白质/脂肪/碳水）占比展示

### 2.4 饮食建议
根据中国居民膳食指南（2022）给出建议：
- 成年男性日均推荐热量 **2250 kcal**，女性 **1800 kcal**
- 三餐配比建议：早餐 30%、午餐 40%、晚餐 30%
- 营养素配比建议：碳水 50-65%、蛋白质 10-15%、脂肪 20-30%
- 当日总热量超出推荐值 20% → 提示"摄入偏高，建议减少高热量食物"
- 当日蔬菜水果类摄入不足 → 提示"建议增加蔬菜水果摄入"
- 晚餐热量占比过高 → 提示"晚餐热量偏高，建议适当减少晚餐摄入"

---

## 3. 数据模型

### 3.1 食物表（food）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| name | VARCHAR(100) | NOT NULL | 食物名称 |
| category | VARCHAR(50) | NOT NULL | 分类（主食/肉类/蔬菜/水果/饮品/零食） |
| calories | INT | NOT NULL | 每单位热量（kcal） |
| unit | VARCHAR(20) | NOT NULL | 计量单位（g/个/碗/杯/份） |
| default_quantity | DECIMAL(10,1) | DEFAULT 100 | 默认份量 |
| protein | DECIMAL(5,1) | DEFAULT 0 | 每单位蛋白质含量（g） |
| fat | DECIMAL(5,1) | DEFAULT 0 | 每单位脂肪含量（g） |
| carbs | DECIMAL(5,1) | DEFAULT 0 | 每单位碳水含量（g） |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

### 3.2 餐食记录表（meal_record）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| record_date | DATE | NOT NULL | 记录日期 |
| meal_type | VARCHAR(10) | NOT NULL | 餐别（BREAKFAST/LUNCH/DINNER） |
| food_id | BIGINT | FK → food.id | 食物ID |
| food_name | VARCHAR(100) | NOT NULL | 食物名称（冗余，便于展示） |
| quantity | DECIMAL(10,1) | NOT NULL | 食用份量 |
| total_calories | INT | NOT NULL | 小计热量 = food.calories × (quantity / food.default_quantity) |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

---

## 4. API 接口设计

### 4.1 食物管理

| 方法 | 路径 | 说明 |
|------|------|------|
| **GET** | `/api/foods` | 食物列表，支持 `?keyword=xxx&category=xxx` |
| **GET** | `/api/foods/{id}` | 单个食物详情 |
| **POST** | `/api/foods` | 新增食物 |
| **PUT** | `/api/foods/{id}` | 更新食物 |
| **DELETE** | `/api/foods/{id}` | 删除食物 |

### 4.2 餐食管理

| 方法 | 路径 | 说明 |
|------|------|------|
| **GET** | `/api/meals?date=2024-01-01` | 某天所有餐食记录 |
| **POST** | `/api/meals` | 新增一条餐食记录 |
| **DELETE** | `/api/meals/{id}` | 删除一条记录 |

### 4.3 统计与建议

| 方法 | 路径 | 说明 |
|------|------|------|
| **GET** | `/api/meals/summary?date=2024-01-01` | 当日总热量 + 三餐分类汇总 + 营养素汇总 |
| **GET** | `/api/meals/suggestion?date=2024-01-01` | 饮食建议（基于推荐值对比） |

---

## 5. 前端页面设计

### 5.1 首页 Dashboard（路由 `/`）
```
┌────────────────────────────────────────────┐
│  [日期选择器 ◀ 2024-01-01 ▶]               │
├──────────┬──────────┬──────────────────────┤
│ 早餐卡片  │ 午餐卡片  │ 晚餐卡片              │
│ 450 kcal │ 800 kcal │ 500 kcal             │
├──────────┴──────────┴──────────────────────┤
│  今日总热量：1750 kcal  ████████░░ 78%      │
│  蛋白质 15% | 脂肪 25% | 碳水 60%           │
├────────────────────────────────────────────┤
│  饮食建议：                                  │
│  ✅ 总热量在推荐范围内                       │
│  ⚠️ 晚餐热量占比偏高                         │
└────────────────────────────────────────────┘
```

### 5.2 记录餐食（路由 `/record`）
```
┌────────────────────────────────────────────┐
│  餐别：[早餐 ▾]    日期：2024-01-01         │
├────────────────────────────────────────────┤
│  🔍 搜索食物...                             │
│  [主食] [肉类] [蔬菜] [水果] [饮品]          │
├────────────────────────────────────────────┤
│  🍚 米饭        116 kcal/100g    [+ 添加]   │
│  🥚 鸡蛋        144 kcal/个      [+ 添加]   │
│  🥦 西兰花       34 kcal/100g    [+ 添加]   │
├────────────────────────────────────────────┤
│  已选食物（早餐）：                           │
│  米饭 ×1.5份（174 kcal）   [✕]              │
│  鸡蛋 ×2份（288 kcal）     [✕]              │
│                           合计：462 kcal     │
│  [保存记录]                                 │
└────────────────────────────────────────────┘
```

### 5.3 食物管理（路由 `/foods`）
```
┌────────────────────────────────────────────┐
│  🔍 搜索...    [全部分类 ▾]    [+ 添加食物]  │
├────────────────────────────────────────────┤
│  名称    │ 分类  │ 热量/单位  │ 蛋白质│ 操作  │
│  米饭    │ 主食  │ 116/100g  │ 2.6g  │ ✎ 🗑  │
│  鸡蛋    │ 肉类  │ 144/个    │ 13.3g │ ✎ 🗑  │
│  ...     │ ...  │ ...       │ ...   │       │
└────────────────────────────────────────────┘
```

---

## 6. 项目目录结构

```
calorie-tracker/
├── 需求文档.md                        # 本文档
│
├── backend/                           # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/calorie/
│       ├── CalorieApplication.java    # 启动类
│       ├── controller/
│       │   ├── FoodController.java
│       │   └── MealController.java
│       ├── service/
│       │   ├── FoodService.java
│       │   └── MealService.java
│       ├── mapper/
│       │   ├── FoodMapper.java
│       │   └── MealRecordMapper.java
│       ├── entity/
│       │   ├── Food.java
│       │   └── MealRecord.java
│       └── config/
│           └── CorsConfig.java        # 跨域配置
│   └── src/main/resources/
│       ├── application.yml
│       └── db/
│           └── schema.sql             # 建表 + 初始数据
│
└── frontend/                          # Vue 3 前端
    ├── package.json
    ├── vite.config.ts
    ├── index.html
    └── src/
        ├── main.ts
        ├── App.vue
        ├── router/index.ts
        ├── api/
        │   ├── food.ts               # 食物 API 封装
        │   └── meal.ts               # 餐食 API 封装
        ├── views/
        │   ├── Dashboard.vue         # 首页概览
        │   ├── RecordMeal.vue        # 记录餐食
        │   └── FoodManage.vue        # 食物管理
        └── components/
            ├── CalorieChart.vue      # 热量环图
            └── FoodCard.vue          # 食物选择卡片
```

---

## 7. 初始数据

预置约 30 种常见中国食物，覆盖以下分类：

| 分类 | 示例食物 |
|------|----------|
| 主食 | 米饭、馒头、面条、小米粥、全麦面包、红薯 |
| 肉类 | 鸡蛋、鸡胸肉、猪瘦肉、牛肉、虾仁、三文鱼 |
| 蔬菜 | 西兰花、番茄、黄瓜、菠菜、胡萝卜、大白菜 |
| 水果 | 苹果、香蕉、橙子、葡萄、西瓜、草莓 |
| 饮品 | 全脂牛奶、豆浆、酸奶、橙汁 |
| 零食 | 核桃、杏仁、黑巧克力、饼干 |

---

## 8. 非功能需求

- 前后端分离，RESTful API 通信
- 后端统一返回 JSON 格式：`{"code": 200, "message": "success", "data": ...}`
- 前端支持移动端响应式布局
- 后端跨域配置允许前端开发端口访问
